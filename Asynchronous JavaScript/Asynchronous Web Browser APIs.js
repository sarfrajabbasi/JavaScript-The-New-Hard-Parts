function printHello(){
    console.log("Hello");
}

setTimeout(printHello,1000);
// it's not interest in JS but it is instead a facade function.happening outside of JS.(web browser)

console.log("Me first!");

// these features that happen outside of our JS engine itself.these are features of web browser.Js is little feature of web browser.

// any feature that's not in my own runtime,in my own engine is something interface with. so we call it an API.so we intreact outside of JS.